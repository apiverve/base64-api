name: apiverve_base64
description: Base64 Encoder/Decoder is a simple tool for encoding and decoding Base64 strings. It supports both encoding text to Base64 and decoding Base64 back to text.
version: 1.1.14
homepage: https://apiverve.com/marketplace/base64?utm_source=dart&utm_medium=homepage
repository: https://github.com/apiverve/base64-api/tree/main/dart

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.1.0
  http_parser: ^4.0.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
